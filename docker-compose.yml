version: '3.8'

services:
  detection-api:
    build: ./detection-api
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - detection-api
